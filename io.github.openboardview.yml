id: io.github.openboardview
runtime: org.freedesktop.Platform
runtime-version: "24.08"
sdk: org.freedesktop.Sdk
command: openboardview
finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --filesystem=home

modules:
  - name: openboardview
    buildsystem: cmake-ninja
    config-opts:
      - -DFLATPAK=ON
    sources:
      - type: git
        url: https://github.com/vlad-sharpie/OpenBoardView
        branch: master
